# Маскирование перед отправкой в Z.AI. Вынесено из requirements.txt отдельно:
# это пара сотен мегабайт колёс ради одной необязательной возможности, и сервис
# обязан собираться и работать без них, когда MASKING_ENABLED=false.
#
# CPU-сборка torch, а не обычная с PyPI: та тянет за собой CUDA-рантайм на
# ~800 МБ, который в контейнере без видеокарты не нужен никому.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.14.0
transformers==5.16.1
